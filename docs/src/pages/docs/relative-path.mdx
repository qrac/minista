---
title: Relative Path
layout: docs
---

サブディレクトリ設置の LP など、全体を相対パスに変換する必要がある場合はコンフィグの `base` を空文字もしくは `./` に設定します。

```ts
import { defineConfig } from "minista"

export default defineConfig({
  base: "./",
})
```

これによってビルドした CSS・JS・画像などへのパスがすべて相対パスに変換されます。
