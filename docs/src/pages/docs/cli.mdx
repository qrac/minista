---
layout: docs
title: CLI
prevTitle: "TypeScript"
prevUrl: "/docs/typescript"
nextTitle: "Config"
nextUrl: "/docs/config"
---

# CLI

ministaのコマンドは基本的にViteと同じで、`vite` のラッパーのように動作します。

ただし、`minista build` は**自動でSSRビルドと通常ビルドを連続で行います**。通常ビルドの前にJSXをNode.jsで扱えるJavaScriptに変換する必要があるからです。

## Table of Contents

contents list

## Commands

|コマンド                 |内容                                           |
| ------------------------ | ---------------------------------------------- |
| `minista [root]`         |開発モード（`Ctrl + C` で停止）                |
| `minista build [root]`   |静的書き出し（SSRビルド後に連続で通常ビルド）|
| `minista preview [root]` |静的書き出し後の動作確認                       |

## Options

[ViteのCLIコマンドオプション](https://ja.vitejs.dev/guide/cli.html)がすべて使えるほか、以下の独自オプションがあります。

|独自のオプション|内容                                              |
| ---------------- | ------------------------------------------------- |
| `--oneBuild`     | ministaのビルドを1回に制限（連続ビルドしない）|
