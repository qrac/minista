---
title: MDX・Markdown
layout: docs
---

# MDX・Markdown

MDX・Markdownは`pluginSsg`に統合され、デフォルトで利用できます。

## Table of Contents

contents list

## v5での処理

MDX／MarkdownからJavaScript moduleへの変換には`@mdx-js/mdx`を直接使用します。compilerとprocessorは最初の対象moduleが読み込まれたときに遅延ロードされ、Markdown用とMDX用のprocessorが再利用されます。

## How To Use

```js title="./minista.config.js"
import { pluginSsg } from "minista"

export default {
  plugins: [pluginSsg()],
}
```

```mdx title="./src/pages/index.mdx"
# heading

## heading2

paragraph
```

## Options

```js title="Default"
pluginSsg({
  mdx: {
    remarkPlugins: [],
    rehypePlugins: [],
  },
})
```

オプションは`development`、`format`、`SourceMapGenerator`を除く[@mdx-js/mdx](https://www.npmjs.com/package/@mdx-js/mdx)の`CompileOptions`と同一です。

remarkPluginsにremark-frontmatterまたはremark-mdx-frontmatterを用いると、デフォルトの設定を上書きできます。

## Frontmatter

フロントマターは `props` としてLayoutやPageで使用可能です。`draft` を `true` にしたページは本番ビルドから除外されます。

```mdx title="./src/pages/index.mdx"
---
title: Index
draft: false // true = Don't build the page
---

# {props.title}
```

## Component

MDX・Markdownのファイルはレイアウトテンプレートやページテンプレート内でコンポーネントとして使用できます。

```jsx title="./src/pages/index.jsx"
import Content from "./index.mdx"

export default function () {
  return <Content />
}
```
