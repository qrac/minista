---
title: Public
layout: docs
---

`public` ディレクトリ配下に配置したアセットはビルドの影響を受けることなく、開発時には `/` として配信され、ビルド時には `outDir` のルートにコピーされます。

主にファビコン・OGP 画像の設置などが想定されます。

```sh
└── public
    └────── favicon.ico
```
