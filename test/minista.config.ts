import { defineConfig } from "minista"

export default defineConfig({
  entry: "src/assets/script.ts",
  /*beautify: {
    useCss: true,
    useJs: true,
  },
  vite: {
    esbuild: {
      minify: false,
      minifySyntax: false,
    },
    build: {
      minify: false,
    },
  },*/
})
