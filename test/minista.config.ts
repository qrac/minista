import { defineConfig } from "minista"

export default defineConfig({
  entry: "src/assets/script.ts",
  /*vite: {
    esbuild: {
      minify: false,
      minifySyntax: false,
    },
    build: {
      minify: false,
    },
  },*/
})
